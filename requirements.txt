# CareThread runtime dependencies (Lambda + local)
boto3>=1.34.0
botocore>=1.34.0
pydantic>=2.6.0
PyYAML>=6.0.1
jsonschema>=4.21.0
pypdf>=4.0.0
pillow>=10.2.0

# The PDF rasteriser (pypdfium2) is deliberately not here: it ships in the
# PdfRenderLayer attached to RasteriseFn alone, so the other functions do not
# carry a PDF engine they never call. See carethread/infra/layers/pdf/.
